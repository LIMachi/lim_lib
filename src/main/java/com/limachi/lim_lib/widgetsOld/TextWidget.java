package com.limachi.lim_lib.widgetsOld;

/*
@SuppressWarnings({"unused", "UnusedReturnValue"})
@OnlyIn(Dist.CLIENT)
public class TextWidget extends BaseWidget<TextWidget> {

    public enum Positioning {
        TOP_LEFT,
        TOP_CENTER,
        TOP_RIGHT,
        MIDDLE_LEFT,
        CENTERED,
        MIDDLE_RIGHT,
        BOTTOM_LEFT,
        BOTTOM_CENTER,
        BOTTOM_RIGHT
    }

    public TextWidget(int x, int y, int w, int h, Positioning pos, Component text) {
        super(x, y, w, h, texture, cutout, simpleBlit);
    }
}
*/